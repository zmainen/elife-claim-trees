# bouyeure-2026-fear-rsa — what each format carries

30 claims, 83 typed relations between them.

One source, three targets. Each row is a relation type the paper's claim tree uses; each column is what became of it.

| Relation | In the tree | MIRA | OXA | Discourse Graphs |
|---|---:|---|---|---|
| `supports` | 19 | → supports | kept | kept |
| `requires` | 13 | **dropped** | kept | dropped |
| `scopes` | 11 | **dropped** | kept | dropped |
| `extends` | 6 | → supports | kept | kept |
| `entails` | 6 | **dropped** | kept | dropped |
| `derived-from` | 6 | **dropped** | kept | dropped |
| `tests` | 5 | → supports | kept | kept |
| `interprets` | 5 | **dropped** | kept | dropped |
| `validates` | 4 | → supports | kept | kept |
| `dissociates-with` | 4 | → opposes | kept | kept |
| `enables-method` | 3 | **dropped** | kept | dropped |
| `confirms` | 1 | → supports | kept | kept |

## What MIRA drops

**44 of 83 relations (53%) have no MIRA predicate and are absent from the strict export.**

- `requires` (13) — a claim depends on another holding
- `scopes` (11) — a scope constraint governs another claim's validity
- `entails` (6) — a hypothesis entails its prediction — the deductive step
- `derived-from` (6) — a prediction derived from its hypothesis
- `interprets` (5) — one claim interprets another
- `enables-method` (3) — a result makes a downstream method possible

## What MIRA flattens

`tests`, `confirms`, `validates`, `extends` and `replicates` all become `mira:supports`; `contradicts`, `rules-out` and `dissociates-with` all become `mira:opposes`. The relation survives; the reason it was drawn does not.

- `supports` (19)
- `extends` (6)
- `tests` (5)
- `validates` (4)
- `dissociates-with` (4)
- `confirms` (1)

## What no format carries

Verification — that a claim was checked, by what code, against what deposited data, with what result beside the published value. None of the three has a node for it.

This paper's records: 13 unattempted, 4 partial, 4 verified, 1 blocked.

---

Generated by `scripts/formats_report.py` from the files in `exports/`, not from the converters' mapping tables — a table says what a converter intends, the export says what it did.
