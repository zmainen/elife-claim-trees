# kolb-2026-igabasnfr2 — what each format carries

21 claims, 90 typed relations between them.

One source, three targets. Each row is a relation type the paper's claim tree uses; each column is what became of it.

| Relation | In the tree | MIRA | OXA | Discourse Graphs |
|---|---:|---|---|---|
| `scopes` | 27 | `cg:scopes`, neutral | kept | dropped |
| `dissociates-with` | 12 | `cg:dissociates-with`, neutral | kept | dropped |
| `confirms` | 9 | `cg:confirms`, under `mira:supports` | kept | kept |
| `supports` | 9 | `cg:supports`, under `mira:supports` | kept | kept |
| `enables-method` | 7 | `cg:enables-method`, neutral | kept | dropped |
| `tests` | 7 | `cg:tests`, neutral | kept | kept |
| `entails` | 5 | `cg:entails`, neutral | kept | dropped |
| `validates` | 4 | `cg:validates`, under `mira:supports` | kept | kept |
| `requires` | 3 | `cg:requires`, neutral | kept | dropped |
| `derived-from` | 3 | — | kept | dropped |
| `extends` | 2 | `cg:extends`, under `mira:supports` | kept | kept |
| `interprets` | 1 | `cg:interprets`, neutral | kept | dropped |
| `rules-out` | 1 | `cg:rules-out`, under `mira:opposes` | kept | kept |

## What MIRA has no predicate for — and what happens instead

**62 of 90 relations (69%) are neither support nor opposition.** They are not dropped and not flattened. MIRA imports a Discourse Graphs base schema in which relations are definable, and its `AbstractRelationDef` is a neutral root — it carries no supporting or opposing commitment — so each is declared in the document with a domain, a range and a description, and the edges are typed by that declaration.

- `cg:scopes` (27) — a scope constraint governs another claim's validity
- `cg:dissociates-with` (12) — the source and target jointly establish a dissociation — two claims whose difference across a condition, region, population or measure is itself the finding, neither bearing on the other's truth (symmetric)
- `cg:enables-method` (7) — a result makes a downstream method possible
- `cg:entails` (5) — a hypothesis entails its prediction — the deductive step
- `cg:requires` (3) — a claim depends on another holding
- `cg:derived-from` (3) — a prediction derived from its hypothesis (inverse of entails)
- `cg:interprets` (1) — one claim interprets another

Declaring them under `mira:supports` would have been worse than dropping them: it would assert that a boundary condition is evidence *for* the claim it limits, which reverses the meaning.

## What a reader who knows only core MIRA sees

Every relation keeps its own type — nothing is flattened into `supports`. 25 of the 87 edges are declared under `mira:supports` or `mira:opposes`, so a reader that follows only those two still gets their direction; the reason the edge was drawn is in the declaration rather than lost.

- `scopes` (27) — neutral
- `dissociates-with` (12) — neutral
- `confirms` (9) — under `mira:supports`
- `supports` (9) — under `mira:supports`
- `enables-method` (7) — neutral
- `tests` (7) — neutral
- `entails` (5) — neutral
- `validates` (4) — under `mira:supports`
- `requires` (3) — neutral
- `extends` (2) — under `mira:supports`
- `interprets` (1) — neutral
- `rules-out` (1) — under `mira:opposes`

## What MIRA genuinely cannot carry

3 `derived-from` relations are not emitted as edges. This is not loss: `derived-from` is declared `owl:inverseOf` `entails`, and MIRA never materialises the reverse direction — its own 942-node demo graph emits no inverse edges either. A reader recovers each one from the forward edge and the declaration.

Beyond those, nothing. Every relation in this paper reaches the export, either as an edge or as the declared inverse of one.

## What no format carries

Verification — that a claim was checked, by what code, against what deposited data, with what result beside the published value. None of the three has a node for it.

This paper's records: 12 blocked, 3 verified, 1 partial.

---

Generated by `scripts/formats_report.py` from the files in `exports/`, not from the converters' mapping tables — a table says what a converter intends, the export says what it did.
