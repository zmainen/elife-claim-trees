{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node scripts/build-data.js && astro dev",
    "dev:all": "CORPUS=all node scripts/build-data.js && astro dev",
    "build": "node scripts/build-data.js && node scripts/check-frames.js && astro build",
    "build:elife": "CORPUS=elife node scripts/build-data.js && node scripts/check-frames.js && astro build",
    "build:all": "CORPUS=all node scripts/build-data.js && node scripts/check-frames.js && astro build",
    "build:share": "CORPUS=all node scripts/build-data.js && node scripts/check-frames.js && astro build --site http://localhost:8000 --base /",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.4",
    "@astrojs/react": "^5.0.2",
    "@dagrejs/dagre": "^3.0.0",
    "@shikijs/rehype": "^4.0.2",
    "@tailwindcss/vite": "^4.2.2",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "astro": "^6.1.1",
    "gray-matter": "^4.0.3",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "reactflow": "^11.11.4",
    "tailwindcss": "^4.2.2",
    "yaml": "^2.8.3"
  }
}
