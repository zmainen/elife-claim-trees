---
// Retired into the paper's own induction cells.
//
// This page narrated one run on one paper at a general URL. The run belongs to the paper it
// read: each reader's claims, the quote each was found in, the model that answered and the
// prompt that was asked are on that paper's cells for the induction layers. The general half
// — the roles, and what the record does not cover — is at /pipeline/induction.
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
return Astro.redirect(`${base}/papers/gadeke-2026-guilt-insula/reconcile/`, 301);
---
