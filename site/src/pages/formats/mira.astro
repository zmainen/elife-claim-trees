---
// Retired into /pipeline/mira-export.
//
// The MIRA reference describes what one layer of the pipeline produces and what the schema it
// targets requires, so it sits on that layer's page.
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
return Astro.redirect(`${base}/pipeline/mira-export/`, 301);
---
