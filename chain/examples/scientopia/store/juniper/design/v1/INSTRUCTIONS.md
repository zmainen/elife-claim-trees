# Instructions: design

Emit `method` claims: what will be measured, in what data or subjects, with what comparison and
analysis — one claim per measurement. Emit `scope` claims for what the design cannot decide.
Link each method to the prediction it tests with `tests`. Say plainly where a method cannot
distinguish the hypothesis from its rivals.
